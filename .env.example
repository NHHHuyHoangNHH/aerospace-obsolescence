# Lấy từ Google Cloud Console → trang chủ project
GCP_PROJECT=

# Tên bucket GCS — script setup_gcp.sh tạo tự động = PROJECT_ID + "-data"
GCS_BUCKET=

# Tên dataset BigQuery — giữ nguyên như trong setup_gcp.sh
BQ_DATASET=

# Region Singapore — gần VN nhất, giữ nguyên
GCP_REGION=asia-southeast1